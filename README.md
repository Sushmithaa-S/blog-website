A Minimalistic Blog application with CRUD 
🎨 FRONTEND
Used EJS and CSS for the structure and styling, which made it flexible to include dynamic content into the frontend, overcoming the limitations of the static HTML pages.
⚙️ BACKEND
Used Node.js to run JavaScript on Express servers.
Used GET and POST requests to render and redirect webpages.
🗄️DATABASE
Currently, this application use JSON to store user input. I intend to integrate an actual database in future.
